{% extends '::base.html.twig' %}

{% block content %}
{{ tinymce_init() }}
<div class="blog blog-item">
    <form class="form-horizontal" role="form" action="{{ path('AtomicBlogBundle_faststart_editsave')}}" method="post" {{ form_enctype(form) }} class="blogger">
        <div class="form-group">   
 {{ form_widget(form) }}
            </div>
            <input type="hidden" name ="id" value="{{f_item.id}}"/>
            <div class="form-group">  
                <input type="submit" class="btn btn-default" value="Submit">
            </div>
        </form>
    </div>
{% endblock content %}