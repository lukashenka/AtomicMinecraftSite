{% extends 'AtomicBlogBundle::layout.html.twig' %}


{% block content %}
 {% block js_additional  %}
<script src="/js/jquery.min.js"></script>
<script src="/lib/minecraft/Three.js"></script>
<script src="/lib/minecraft/RequestAnimationFrame.js"></script>

{% javascripts '@AtomicUserBundle/Resources/public/js/*' %}
<script type="text/javascript" src="{{ asset_url }}""></script>
{% endjavascripts %}


{% endblock %}


<div class="blog-item">
    <h3>Это вы</h3>
    <div id="steve"></div>

</div>
<div class="blog-item">

    <h3>Изменить скин</h3>
    <form action="{{ path('atomic_tuning_uploadskin') }}" {{ form_enctype(form_skin) }} method="POST">
         {{ form_start(form_skin) }}
         {{ form_errors(form_skin) }}
         {{ form_row(form_skin.file, { 'attr': {'class': 'form-control'} }) }}

            <div>
                <input type="submit" class="btn btn-default" value="Обновить" />
            </div>
        {{ form_end(form_skin) }}
        </form>
    </div>
    <div class="blog-item">
        <h3>Изменить плащ</h3>
        <form action="{{ path('atomic_tuning_uploadskin') }}" {{ form_enctype(form_skin) }} method="POST">
         {{ form_start(form_cloack) }}
         {{ form_errors(form_cloack) }}
         {{ form_row(form_cloack.file, { 'attr': {'class': 'form-control'} }) }}

                <div>
                    <input type="submit" class="btn btn-default" value="Обновить" />
                </div>
        {{ form_end(form_cloack) }}
            </form>
        </div>

        <script type="text/javascript">
            $(document).ready(function() {


                drawSteve(document.getElementById('steve'), "{{user.username}}", "/{{skin.getUploadDir}}");
            }
            );
            </script>
{%endblock %}