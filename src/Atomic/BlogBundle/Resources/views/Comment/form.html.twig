<form class="form-horizontal" action="{{ path('AtomicBlogBundle_comment_create', { 'blog_id' : comment.blog.id } ) }}" method="post" {{ form_enctype(form) }} class="blogger">
    {{ form_widget(form) }}
        <p>
            <input type="submit" value="Добавить" class="btn btn-default" style="
                   float: right;
                   height: 40px;
                   ">
            </p>
        </form>